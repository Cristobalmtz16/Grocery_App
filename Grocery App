shopping_lists = []


class GroceryItem:
  def __init__(self, name): 
    self.name = name
    
 class ShoppingList:
  def __init__(self, name, address):
    def __init__(self, name):
      self.name = name
      self.address = address
      self.grocery_items = []
      
  def add_grocery_item(self, item):
    self.grocery_items.append(item)
   
  def __str__ (self):
    return "%s" % (self_grocery_item)


def display_all_shopping_lists():
  for index in range(0, len(shopping_lists)):
    print(f"{index}. {shopping_lists[index].name")

    
  print("1. Add a shopping list")
  print("2. add item to an existing shopping list")
  print("3. display shopping lists")
  print("q to quit)
  
  
  choice = input("Enter choice: ")
  if choice == "1":
    display_all_shopping_lists()
    name = input("Enter name of shopping list: ")
    address = input("Enter shopping list address: ")
    shopping_list = ShoppingList(name, address)
    shopping_list.append(shopping_list)
    
  elif choice == "2":
    user_input_list_title
    user_input_grocery_title
    user_input_grocery_quantity
    item = GroceryItem(user_input_list_title, user_input_grocery_title, user_input_grocery_quantity
    shopping_list.add_item(item) 
 
  elif choice == "3":
    display_all_shopping_lists()
    for index in range(0, len(display_all_shopping_lists)):
      print(f'{index}. {shopping_lists[index].name}")
     for index in range(0, len(list.grocery_items)
      grocery_item = list.grocery_items[index]
      print(f"{index}. {grocery_item.name}")
      
  elif choice == "q":
    break
  
  
  
  
